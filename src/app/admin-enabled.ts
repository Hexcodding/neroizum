/**
 * Включена ли панель управления в этой сборке.
 *
 * Признак читается из окружения на этапе сборки и превращается сборщиком в
 * обычное `false`. Из-за этого ветка с загрузкой страницы админки становится
 * недостижимой, и её код в клиентский бандл не попадает вообще — а не просто
 * «не открывается по адресу». Разница принципиальная: спрятанный, но
 * присутствующий в бандле экран читается любым желающим.
 *
 * Отсюда следует порядок работы: клиентская сборка идёт без признака, а панель
 * собирается отдельно (`npm run build:admin`) и выкладывается по своему адресу.
 */
export const ADMIN_ENABLED = import.meta.env.VITE_ADMIN === "on";
