/**
 * Голос и живость текста.
 *
 * Зачем отдельный блок. Запрет выдумывать факты — правило необходимое: без него
 * модель насочиняет цен, акций и исследований. Но у запрета есть цена: самый
 * безопасный для модели способ ничего не выдумать — писать общими словами. Так
 * появляются посты вида «Многие предприниматели думают… На самом деле…»: ни
 * одного проверяемого утверждения и ни одной живой строки.
 *
 * Здесь стоит противовес: требование конкретности и ритма. Живость не украшение
 * — пост без неё не дочитывают, и вся польза внутри остаётся непрочитанной.
 */

/**
 * Штамп как правило, а не как строка.
 *
 * Первая версия списка перечисляла точные фразы, и модель обошла её одним
 * синонимом: вместо запрещённого «многие предприниматели думают» написала
 * «многие предприниматели видят». Перечислять синонимы можно бесконечно и
 * всё равно не угадать, поэтому запрещается схема оборота.
 *
 * label — как штамп называется в промпте, человеку и модели. pattern — как он
 * ищется в готовом посте проверкой качества. Оба поля рядом намеренно: запрет
 * в инструкции и проверка результата не должны расходиться.
 */
export interface ClicheRule {
  readonly label: string;
  readonly pattern: RegExp;
}

export const CLICHE_RULES: readonly ClicheRule[] = [
  {
    label: "«многие думают / многие предприниматели считают» — и любые обороты про то, что думает безымянное большинство",
    pattern: /мног\p{L}*\s+(?:\p{L}+\s+){0,2}(?:дума|счита|вид|увере|полага|мечта|бо[яе])\p{L}*/iu,
  },
  {
    label: "«не волшебная палочка», «не волшебная таблетка», «не серебряная пуля», «не панацея»",
    pattern: /волшебн\p{L}*\s+(?:палочк|таблетк)\p{L}*|серебрян\p{L}*\s+пул\p{L}*|панаце\p{L}*/iu,
  },
  {
    label: "«в современном мире», «в эпоху цифровизации», «в наше время»",
    pattern: /в\s+(?:современном\s+мире|эпоху\s+\p{L}+|наше\s+время)/iu,
  },
  {
    label: "«ни для кого не секрет», «как известно», «по мнению экспертов»",
    pattern: /ни\s+для\s+кого\s+не\s+секрет|не\s+секрет,?\s+что|как\s+известно|по\s+мнению\s+экспертов/iu,
  },
  {
    label: "«давайте разберёмся», «в этой статье», «представьте себе»",
    pattern: /давайте\s+разбер|в\s+этой\s+статье|представьте\s+себе/iu,
  },
  {
    label: "«начните с малого», «тестируйте гипотезы», «требует правильного подхода» — совет, который подходит любому бизнесу и потому не помогает никому",
    pattern:
      /начн\p{L}*\s+с\s+малого|тестируйте\s+гипотез\p{L}*|требует\s+(?:правильного\s+подход|системного\s+подход|комплексного\s+подход)\p{L}*/iu,
  },
  {
    label: "«всё гениальное просто», «время — деньги» и прочие поговорки вместо мысли",
    pattern: /вс[её]\s+гениальное\s+просто|время\s*[—-]\s*деньги/iu,
  },
  {
    label: "приветствие «Друзья!» в начале и «Ставьте лайки, если было полезно» в конце",
    pattern: /^друзья[!,]|ставьте\s+лайк\p{L}*|подписывайтесь,?\s+чтобы\s+не\s+пропустить/iu,
  },
];

/** Найденный в тексте штамп — сам фрагмент, чтобы объяснить модели предметно. */
export function findCliche(text: string): string | null {
  for (const rule of CLICHE_RULES) {
    const found = rule.pattern.exec(text);
    if (found !== null) return found[0].trim();
  }
  return null;
}

function clicheLines(): string {
  return CLICHE_RULES.map((rule) => `- ${rule.label};`).join("\n");
}

export const VOICE_BLOCK = `ГОЛОС И ЖИВОСТЬ ТЕКСТА.
Пост пишет человек, который делом занимается, а не пересказывает общеизвестное. Читатель должен узнать в тексте живого автора, а не «контент-план».

ОТКУДА БЕРЁТСЯ ЖИВОСТЬ:
- начинай с конкретного: сцена, цифра из данных о бизнесе, реплика клиента, собственная ошибка и вывод из неё. Рассуждение о том, что «многие думают», — это разогрев, а не начало;
- одно наблюдение или один конфликт на пост, разобранный до конца, вместо обзора темы с трёх сторон;
- ритм неровный: короткая фраза после длинной, вопрос посреди объяснения, абзац из одной строки там, где нужна пауза;
- слова обычные, разговорные. Так объясняют коллеге за столом, а не пишут в отчёте;
- прямая речь уместна: и своя, и клиента. «Мне сказали: „дорого“» работает лучше, чем «клиенты часто возражают по цене»;
- признание сомнения и ошибки — сильный ход, а не слабость. Оно и отличает автора от рекламного текста.

ЕСЛИ ФАКТОВ МАЛО. Выдумывать по-прежнему запрещено. Но нехватка данных о бизнесе — не повод уходить в обобщения: пиши о процессе, о выборе, о том, как решение принимается и что при этом мешает. Это конкретно и ничего не сочиняет.

СОВЕТ БЕЗ АДРЕСАТА — ТОЖЕ ВОДА. «Начните с малого», «подходите системно», «тестируйте гипотезы» подходят любому бизнесу и потому не помогают никому. Пиши то, что верно именно для этой нишы и этой аудитории: что сделать в понедельник, на что посмотреть в своих цифрах, какую фразу сказать клиенту.

ЗАПРЕЩЁННЫЕ ОБОРОТЫ, ни в какой форме и ни с какими синонимами:
${clicheLines()}

Эти обороты проверяются программой по образцу, а не по точной фразе: замена слова на близкое по смыслу не помогает. Пост с любым из них возвращается на переделку.`;
